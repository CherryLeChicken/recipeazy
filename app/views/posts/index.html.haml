- if user_signed_in?
	%p#intro
		Hello
		= current_user.name 
		!
		%br
		%span
			Share your own money-saving healthy recipes, or see the recipes others like!
- else
	%p#intro
		Welcome to Recipeazy!
		%br
		%span
			What's your favourite money-saving recipe? Sign up to share!
#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size